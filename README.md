# ecommerce-store
## Run locally
First,
```bash
cd backend
npm start
```
In new terminal
```bash
cd frontend
npm start
```
Redirects to http://localhost:3000

---
## Run using docker 
In the root folder,
```bash
docker compose up -d
```
Then, go to http://localhost:5000
